import { ForEachDirective } from './for-each.directive';

describe('ForEachDirective', () => {
  it('should create an instance', () => {
   // const directive = new ForEachDirective();
    expect(directive).toBeTruthy();
  });
});
